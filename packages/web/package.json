{
  "name": "@resonance/web",
  "version": "0.1.0",
  "private": true,
  "description": "The static PWA: boards, resonance, trend memory, BYOK summaries, search, theming.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "mock": "node scripts/mock-api.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0",
    "@preact/signals": "^2.11.2",
    "@resonance/schema": "workspace:*",
    "preact": "^10.29.8",
    "tweetnacl": "^1.0.3",
    "@noble/hashes": "^2.4.0",
    "@resonance/channels": "workspace:*"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "happy-dom": "^20.14.5",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
